<Window x:Class="MidnaHelmetHelperWPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:h="http://helix-toolkit.org/wpf"
        Title="Fused Shadow Helper" Height="480" Width="640"
		WindowStyle="None" AllowsTransparency="True"
		 ResizeMode="CanResizeWithGrip"
    Background="Transparent"
		Topmost = "True"
		
		>

	<Window.Resources>
		<LinearGradientBrush x:Key="WindowFrameBrush" StartPoint="0,0.5" EndPoint="1,0.5">
			<GradientStop Color="#FF353535" Offset="0.4"/>
			<GradientStop Color="Black" Offset="1"/>
		</LinearGradientBrush>
	</Window.Resources>
	<Border Background="#50FFFFFF" CornerRadius="5" BorderBrush="{StaticResource WindowFrameBrush}" BorderThickness="2,0,2,2">
		<Grid ShowGridLines="True">
			<Grid.RowDefinitions>
				<RowDefinition Height="30" />
				<RowDefinition/>
				<RowDefinition Height="auto" />
			</Grid.RowDefinitions>
			<Border Grid.Row="0" Background="{StaticResource WindowFrameBrush}" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"
              CornerRadius="5,5,0,0" Margin="-1,0,-1,0" MouseLeftButtonDown="DragWindow">
				<Grid>
					<Button Content="PASTE CLIPBOARD" Width="94" Style="{StaticResource customButton}" HorizontalAlignment="Left" Margin="90,1,0,-1"/>
					<Button Content="LOCK" Width="61" Style="{StaticResource customButton}" HorizontalAlignment="Left" Margin="189,0,0,0"/>

					<TextBlock Foreground="White" FontWeight="Bold" VerticalAlignment="Center" Margin="10,0,130,0"
            Text="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type Window}}, Path=Title}"/>
					<Slider Name="OpacitySlider" Width="100" Height="20"  Value="100"
 Background="Black" Maximum="100" Minimum="0" Margin="273,5,265,5" ValueChanged="SlideOpacity">
					</Slider>
					<Button Name="GFHFGH" Content="TRANSPARENT" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="515,0,0,0" FontSize="7"
                  Width="27" Height="15" Padding="0"/>

					<Button  Name="PasteBackgroundButton" Content="PASTE BG" Width="77" Style="{StaticResource customButton}" HorizontalAlignment="Left" Margin="473,1,0,-1"/>
					<Button  Name="MinimizeButton" Content="-" Width="38" Style="{StaticResource customButton}" HorizontalAlignment="Left" Margin="595,1,0,-1"/>
					<Button  Name="ImageButton" Content="X" Width="38" Style="{StaticResource customButton}" HorizontalAlignment="Left" Margin="555,1,0,-1"/>
				</Grid>

			</Border>
			<h:HelixViewport3D Grid.Row="1" MouseLeftButtonDown="Window_MouseLeftButtonDown" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">

				<h:DefaultLights/>
				<ModelVisual3D x:Name="model">
					<ModelVisual3D.Transform>
						<Transform3DGroup>
							<RotateTransform3D>
								<RotateTransform3D.Rotation>
									<AxisAngleRotation3D Axis="1,0,0" Angle="{Binding varX}"/>
								</RotateTransform3D.Rotation>
							</RotateTransform3D>
							<RotateTransform3D>
								<RotateTransform3D.Rotation>
									<AxisAngleRotation3D Axis="0,1,0" Angle="{Binding varY}"/>
								</RotateTransform3D.Rotation>
							</RotateTransform3D>
							<RotateTransform3D>
								<RotateTransform3D.Rotation>
									<AxisAngleRotation3D Axis="0,0,1" Angle="{Binding varZ}"/>
								</RotateTransform3D.Rotation>
							</RotateTransform3D>
						</Transform3DGroup>
					</ModelVisual3D.Transform>
				</ModelVisual3D>

			</h:HelixViewport3D>
		</Grid>

	</Border>


</Window>